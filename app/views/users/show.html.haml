%p
  %b Name:
  = @user.name
%p
  %b Email:
  = @user.email

%p #{@user.name}'s Books
- if @user_books.blank?
  %p You haven't added any books yet.
- else
  = render :partial => 'books/index_partial', :locals => { :books => @user_books, :show_user => @user.id }

%br

- if @can_edit
  = link_to 'Add a new Book', new_book_path
  \|
  = link_to 'Edit Profile', edit_user_path(@user)
  \|
= link_to 'Back', :back
